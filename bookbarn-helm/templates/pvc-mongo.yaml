apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bookbarn.fullname" . }}-mongo-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.mongo.storage }}
