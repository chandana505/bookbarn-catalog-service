apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "bookbarn.fullname" . }}-pg-pvc
  namespace: {{ .Values.namespace }}
spec:
  accessModes: ["ReadWriteOnce"]
  resources:
    requests:
      storage: {{ .Values.postgres.storage }}
